#include <stdio.h>
int main()
{
    /* Write a program to calculate area of circle. */
    float r;
    printf("Enter the value of radius.");
    scanf("%f",&r);
    printf("The area of circle with radius %f is %f",r,22*r*r/7);
}
